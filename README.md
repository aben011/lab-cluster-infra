# lab-cluster-infra

Cluster-wide infrastructure applied to every workload cluster (storage, networking, TLS, backups). The only repo trusted with cluster-scoped resources — application repos are namespace-scoped only.

